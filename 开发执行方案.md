# 易合同 — 开发执行方案（第1-8周）

> 本文档是**唯一的开发执行指令**。
> 所有规划、调研、架构文档中的结论已汇入此处，按阶段、按任务、按要求逐一落地。
> 不跑偏、不做不在清单里的事、不擅自增加功能。

---

## Phase 1 — 清理与加固（第1周：7/09-7/15）

### 目标
消除代码中的技术隐患，让构建过程零错误、零警告，为后续开发打下干净的基础。

### 1.1 删除重复文件

**任务描述**：清除 `_duplicates_raw/` 目录中的 30+ 无用副本文件。

**涉及文件**：
- 删除整个目录 `/Users/quanyimin/Documents/易合同 AI 助手/_duplicates_raw/`

**验收标准**：
- ✅ `_duplicates_raw/` 目录不再存在
- ✅ `git rm -r _duplicates_raw/` 后 `npm run dev` 正常启动
- ✅ 项目根目录 `ls` 中不再出现该目录

### 1.2 恢复 TypeScript 严格模式

**任务描述**：修改 `next.config.mjs`，开启 TypeScript 编译检查，然后逐文件修复所有类型错误。

**涉及文件**：
- `/Users/quanyimin/Documents/易合同 AI 助手/next.config.mjs`

**修改内容**：
```diff
- typescript: { ignoreBuildErrors: true },
+ // 删除这一行即可恢复默认严格模式
```

**执行步骤**：
1. 先删除 `typescript: { ignoreBuildErrors: true }` 这一行
2. 运行 `npx next build` 记录所有类型错误
3. 按文件逐个修复，直到 `npx next build` 零错误通过

**修复范围**（根据错误数量确定，预计需修复的典型位置）：
- `lib/` 下的工具函数——补充缺失的类型标注
- `app/` 下的 API 路由——修正 Prisma 查询的返回类型
- `components/` 下的组件——补充 Props 接口
- `app/dashboard/` 下的页面——修正状态类型

**验收标准**：
- ✅ `npx next build` 零错误、零警告通过
- ✅ `tsconfig.json` 中 `strict: true` 保持开启
- ✅ next.config.mjs 中不再有 `ignoreBuildErrors`

### 1.3 恢复 ESLint 检查

**任务描述**：开启 ESLint 构建时检查，修复所有 lint 问题。

**涉及文件**：
- `/Users/quanyimin/Documents/易合同 AI 助手/next.config.mjs`

**修改内容**：
```diff
- eslint: { ignoreDuringBuilds: true },
+ // 删除这一行即可恢复默认检查
```

**验收标准**：
- ✅ `npx next build` 中 ESLint 阶段零警告
- ✅ `npm run lint` 零错误通过

### 1.4 统一 API 错误处理

**任务描述**：抽取统一的 API 错误处理工具函数，所有 API 路由接入统一格式。

**新建文件**：
- `/Users/quanyimin/Documents/易合同 AI 助手/lib/api-error.ts`

**要求**：
```typescript
// lib/api-error.ts

export class ApiError extends Error {
  constructor(
    public statusCode: number,
    public message: string,
    public details?: unknown
  ) {
    super(message);
  }
}

export function handleApiError(error: unknown) {
  if (error instanceof ApiError) {
    return Response.json(
      { success: false, error: error.message, details: error.details },
      { status: error.statusCode }
    );
  }
  
  console.error('[API Error]', error);
  return Response.json(
    { success: false, error: '服务器内部错误' },
    { status: 500 }
  );
}

export function successResponse<T>(data: T) {
  return Response.json({ success: true, data });
}
```

**改动范围**：所有 `app/api/**/route.ts` 文件。

**验收标准**：
- ✅ 所有 API 返回格式统一为 `{ success: boolean, data?: T, error?: string }`
- ✅ 400/403/404/500 状态码正确使用
- ✅ 关键日志有 console.error 输出

---

## Phase 2 — 数据模型增强（第2周：7/16-7/22）

### 目标
在现有 Prisma Schema 基础上，增加个人空间、收支方向、插件注册表三个关键模型。

### 2.1 补全缺失的模型

**任务描述**：确认并补全 Prisma Schema 中缺失的模型（Tenant, UserTenantRole, IndustryVersion 等）。

**涉及文件**：
- `/Users/quanyimin/Documents/易合同 AI 助手/prisma/schema.prisma`

**需要验证的模型**（如果缺失则补充）：
```
model Tenant {
  id                String   @id @default(cuid())
  name              String
  type              String   // PERSONAL | INDIVIDUAL | ENTERPRISE
  sceneType         String   @default("GENERAL")
  industry          String?
  creditCode        String?
  contactName       String?
  contactPhone      String?
  industryVersionId String?  @map("industry_version_id")
  planId            String?
  createdAt         DateTime @default(now()) @map("created_at")
  updatedAt         DateTime @updatedAt @map("updated_at")

  roles    UserTenantRole[]
  enabledScenes TenantEnabledScene[]

  @@map("tenants")
}

model UserTenantRole {
  id        String   @id @default(cuid())
  userId    String   @map("user_id")
  tenantId  String   @map("tenant_id")
  role      String   @default("VIEWER")

  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([userId, tenantId])
  @@map("user_tenant_roles")
}

model TenantEnabledScene {
  id        String   @id @default(cuid())
  tenantId  String   @map("tenant_id")
  sceneId   String   @map("scene_id")
  enabled   Boolean  @default(true)
  createdAt DateTime @default(now()) @map("created_at")

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([tenantId, sceneId])
  @@map("tenant_enabled_scenes")
}
```

**验收标准**：
- ✅ 缺失模型已补充
- ✅ `npx prisma generate` 成功
- ✅ `npx prisma db push` 成功

### 2.2 Contract 模型扩展

**任务描述**：在现有 Contract 模型上增加 direction（收支方向）、source（来源）、tenantId（多租户关联）字段。

**涉及文件**：
- `/Users/quanyimin/Documents/易合同 AI 助手/prisma/schema.prisma`

**修改内容**（在现有 Contract 模型中增加字段）：
```prisma
model Contract {
  // 保留现有字段不变...
  
  // 新增字段：
  direction String?  // INCOME | EXPENSE — 收支方向
  source    String   @default("UPLOAD") // UPLOAD | PHOTO | EMAIL | IMPORT | API
  tenantId  String?  @map("tenant_id")  // 多租户关联（空=个人空间）
  
  // 保留现有关系...
  // 新增关系（如果 tenantId 有值）：
  tenant    Tenant?  @relation(fields: [tenantId], references: [id])

  @@index([tenantId])  // 新增索引
  @@index([direction])
  @@index([source])
}
```

**验收标准**：
- ✅ `npx prisma migrate dev` 成功生成新迁移文件
- ✅ 现有合同数据无损迁移（direction 默认 null）
- ✅ 新增索引生效

### 2.3 PersonalSpace 模型

**任务描述**：创建 PersonalSpace 模型，用户注册时自动创建个人空间。

**涉及文件**：
- `/Users/quanyimin/Documents/易合同 AI 助手/prisma/schema.prisma`
- `/Users/quanyimin/Documents/易合同 AI 助手/app/api/auth/register/route.ts`

**模型定义**：
```prisma
model PersonalSpace {
  id      String   @id @default(cuid())
  userId  String   @unique @map("user_id")
  name    String   @default("我的合同")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  user      User       @relation(fields: [userId], references: [id], onDelete: Cascade)
  contracts Contract[] @relation("PersonalContracts")

  @@map("personal_spaces")
}
```

**注册逻辑变更**：用户注册成功后，自动创建 PersonalSpace。

**验收标准**：
- ✅ 新用户注册时自动创建 PersonalSpace
- ✅ 已有用户手动执行 seed 脚本补全
- ✅ Contract 可关联到 PersonalSpace 或 Tenant

### 2.4 PluginDefinition 模型（插件注册表）

**任务描述**：创建插件注册表模型，实现「插件数据化」的第一步——插件定义从文件系统迁移到数据库。

**涉及文件**：
- `/Users/quanyimin/Documents/易合同 AI 助手/prisma/schema.prisma`

**模型定义**：
```prisma
model PluginDefinition {
  id          String   @id @default(cuid())
  code        String   @unique           // 插件编码，如 "landlord", "catering"
  name        String                     // 插件名称，如 "房东收租"
  description String?                    // 插件描述
  icon        String   @default("Package") // lucide-react 图标名
  version     String   @default("1.0.0")
  price       Float    @default(0)       // 年费价格（元），0=免费
  configSchema String?                   // JSON Schema 配置定义
  enabled     Boolean  @default(false)   // 是否上架
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  industryScenes IndustryScene[]

  @@map("plugin_definitions")
}

model IndustryScene {
  id         String @id @default(cuid())
  code       String                    // 场景编码，如 "rent_collection"
  name       String                    // 场景名称，如 "收租管理"
  pluginId   String @map("plugin_id")
  
  plugin     PluginDefinition @relation(fields: [pluginId], references: [id], onDelete: Cascade)

  @@unique([code])
  @@map("industry_scenes")
}
```

**注意**：这个模型建立后，`lib/menu-config.ts` 中的 `SCENE` 常量和菜单配置需要改为从数据库读取。

**验收标准**：
- ✅ PluginDefinition + IndustryScene 模型创建成功
- ✅ 初始数据通过 seed 脚本插入（房东/餐饮/律所/科技 四个插件）

---

## Phase 3 — 个人版 MVP（第3-4周：7/23-8/05）

### 目标
实现搜索、批量导入、Onboarding 三个核心功能，交付可用的个人版 MVP。

### 3.1 搜索功能

**任务描述**：实现合同全文搜索，支持模糊匹配、错别字容忍、按相似度排序。

**涉及文件**：
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/app/api/search/route.ts`
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/components/search/GlobalSearch.tsx`
- `/Users/quanyimin/Documents/易合同 AI 助手/components/navbar.tsx`

**API 要求**：
```typescript
// GET /api/search?q=关键词&type=contract&limit=20
// 响应格式：
{
  success: true,
  data: [
    {
      id, title, serialNumber, partyA, partyB,
      amount, status, direction,
      score: 0.95,          // 相似度得分
      highlightedTitle: "<mark>租房</mark>合同"  // 高亮
    }
  ]
}
```

**后端逻辑**：
- 使用 PostgreSQL pg_trgm 或 SQLite LIKE 进行模糊匹配
- 按相似度降序排列
- 限制返回 20 条
- 支持搜索字段：合同名称、甲方、乙方、合同编号

**前端组件要求**：
- 全局搜索栏（固定在顶部导航栏）
- Ctrl+K / Cmd+K 快捷键聚焦
- 200ms 防抖
- 搜索结果下拉浮层
- 高亮匹配关键词
- 显示金额、方向标签、状态标签
- 点击结果跳转到合同详情页
- 无结果时显示「未找到匹配的合同」

**验收标准**：
- ✅ 输入任意关键词，100ms 内返回结果
- ✅ 合同名称/甲方/乙方/编号任意字段匹配
- ✅ 高亮显示匹配关键词
- ✅ Ctrl+K 快捷键可用
- ✅ 无结果时有友好提示

### 3.2 批量导入（Excel）

**任务描述**：实现 Excel/CSV 批量导入合同，降低用户从其他系统迁移的壁垒。

**涉及文件**：
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/app/api/import/route.ts`
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/app/dashboard/import/page.tsx`
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/lib/import-parser.ts`
- `/Users/quanyimin/Documents/易合同 AI 助手/config/menu.ts`（添加导入菜单项）

**API 要求**：
```typescript
// POST /api/import
// Content-Type: multipart/form-data
// body: { file: File, mapping: { "合同名称":"A列","金额":"B列",... } }

// 响应：
{
  success: true,
  data: {
    total: 150,       // 总记录数
    imported: 148,    // 成功导入数
    failed: 2,        // 失败数
    errors: [         // 失败详情
      { row: 5, reason: "金额格式错误" },
      { row: 23, reason: "日期无效" }
    ]
  }
}
```

**解析逻辑要求**：
- 支持 .xlsx / .xls / .csv 三种格式（mammoth 已安装）
- 支持自动检测列头（第一行作为字段名）
- 支持用户手动映射列到合同字段
- 支持字段：合同名称、甲方、乙方、金额、开始日期、结束日期、类型、备注
- 日期自动解析（多种格式兼容：2026-01-01 / 2026/01/01 / 2026年1月1日）
- 金额自动解析（支持「58万」「1,200,000」「1200000」等格式）
- 导入进度实时反馈（前端轮询或 WebSocket）

**前端页面要求**：
- 拖拽上传区域
- 字段映射配置界面（下拉选择列对应关系）
- 导入进度条 + 实时日志
- 导入结果汇总

**验收标准**：
- ✅ 支持 1000 条以上的 Excel 导入
- ✅ 日期/金额自动解析准确率 > 95%
- ✅ 导入失败有详细错误提示
- ✅ 导入后自动跳转到合同列表

### 3.3 用户引导（Onboarding）

**任务描述**：实现新用户注册后的三步引导流程，让用户快速看到产品价值。

**涉及文件**：
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/components/onboarding/OnboardingWizard.tsx`
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/components/onboarding/steps/Step1Upload.tsx`
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/components/onboarding/steps/Step2AIParse.tsx`
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/components/onboarding/steps/Step3Reminder.tsx`
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/data/sample-contract.json`
- `/Users/quanyimin/Documents/易合同 AI 助手/app/(auth)/register/page.tsx`（注册后跳转到引导）

**引导流程**：
```
注册完成 → 引导页

Step 1: 「上传你的第一份合同」
  - 三大按钮：📷 拍照上传 / 📄 上传文件 / 🎯 使用示例合同
  - 使用示例合同：直接演示，无需用户准备
  - 上传后自动跳转 Step 2

Step 2: 「AI 正在解析...」
  - 进度动画 + 展示识别到的字段
  - 完成后展示提取结果
  - 用户可以手动修正
  
Step 3: 「设置提醒」
  - 「合同到期前 7 天提醒我」开关
  - 「收款/付款提醒」开关
  - 完成按钮 → 进入首页
```

**验收标准**：
- ✅ 注册后自动进入引导流程
- ✅ 三步引导完整可操作
- ✅ 示例合同让用户无需准备即可体验
- ✅ 引导完成后自动跳转首页
- ✅ 跳过引导按钮（右上角）

### 3.4 空状态设计

**任务描述**：所有页面的空状态（无数据时）展示引导性内容，而非空白页面。

**涉及范围**：
- `/Users/quanyimin/Documents/易合同 AI 助手/app/dashboard/page.tsx`（首页无合同）
- `/Users/quanyimin/Documents/易合同 AI 助手/app/dashboard/contracts/page.tsx`（合同列表空）
- `/Users/quanyimin/Documents/易合同 AI 助手/app/dashboard/ledger/page.tsx`（台账空）
- `/Users/quanyimin/Documents/易合同 AI 助手/app/dashboard/reminders/page.tsx`（提醒空）

**设计要求**：
- 每个空状态页面展示：插画 + 标题 + 说明文字 + 操作按钮
- 操作按钮引导用户完成核心 action（上传合同/导入数据/设置提醒）
- 风格统一，不出现空白的表格或列表

**验收标准**：
- ✅ 所有列表页有空状态组件
- ✅ 空状态包含引导性操作按钮
- ✅ 不展示空白表格或无数据白屏

---

## Phase 4 — 搜索增强与插件完善（第5-6周：8/06-8/19）

### 目标
完成搜索功能的全文检索升级，完善房东和餐饮两个行业插件的全功能。

### 4.1 搜索增强（全文检索）

**任务描述**：在 Phase 3 模糊匹配基础上，增加 PostgreSQL 全文检索（tsvector），实现更精准的语义搜索。

**涉及文件**：
- `/Users/quanyimin/Documents/易合同 AI 助手/prisma/schema.prisma`（新增 tsvector 字段）
- `/Users/quanyimin/Documents/易合同 AI 助手/app/api/search/route.ts`（增强）

**要求**：
- Contract 表新增 `searchVector` 字段（tsvector 类型，如 PostgreSQL 可用）
- 用触发器自动维护 `searchVector`（合同创建/更新时自动更新）
- 查询先用全文检索（精确匹配），再用模糊匹配（容错）

**验收标准**：
- ✅ 搜索「租赁合同 张三」能精确匹配到对应合同
- ✅ 搜索「租货合同」（错别字）仍能匹配到「租赁合同」
- ✅ 搜索响应时间 < 200ms

### 4.2 房东插件完善

**任务描述**：将房东插件的三个子功能（水电读数、押金管理、租金提醒）从占位页变为完整功能。

**涉及文件**：
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/app/api/scenes/landlord/meters/route.ts`
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/app/api/scenes/landlord/deposit/route.ts`
- `/Users/quanyimin/Documents/易合同 AI 助手/app/dashboard/landlord/meters/page.tsx`（重写）
- `/Users/quanyimin/Documents/易合同 AI 助手/app/dashboard/landlord/deposit/page.tsx`（重写）

**功能要求**：
- 水电读数：录入/编辑/历史记录/用量趋势图
- 押金管理：押金记录/退还状态/退还流程
- 租金提醒：已实现，增加续签提醒

**验收标准**：
- ✅ 水电读数可录入和查看历史
- ✅ 押金管理可标记退还状态
- ✅ 三个功能与合同关联

### 4.3 餐饮插件完善

**任务描述**：将餐饮插件的子功能（供应商管理、证照管理、月度计划）从占位页变为完整功能。

**涉及文件**：
- `/Users/quanyimin/Documents/易合同 AI 助手/app/dashboard/restaurant/licenses/page.tsx`（重写）
- `/Users/quanyimin/Documents/易合同 AI 助手/app/dashboard/restaurant/plan/page.tsx`（重写）
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/app/api/scenes/restaurant/licenses/route.ts`

**功能要求**：
- 证照管理：上传/到期提醒/续期提醒
- 月度计划：月度付款计划展示/编辑/完成标记

**验收标准**：
- ✅ 证照上传 + 到期提醒
- ✅ 月度付款计划可编辑

---

## Phase 5 — 企业版起步（第7-8周：8/20-9/02）

### 目标
实现企业空间基础功能、支付计费闭环、电子签集成。

### 5.1 企业空间

**任务描述**：实现企业创建、成员管理、空间切换。

**涉及文件**：
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/app/api/tenants/route.ts`
- `/Users/quanyimin/Documents/易合同 AI 助手/components/TenantSwitcher.tsx`（增强）
- `/Users/quanyimin/Documents/易合同 AI 助手/app/dashboard/tenants/page.tsx`（完善）

**功能要求**：
- 创建企业空间（名称/行业/规模）
- 加入企业空间（通过邀请码或链接）
- 个人/企业空间一键切换（顶部导航栏）
- 企业空间成员列表 + 角色设置

**验收标准**：
- ✅ 个人可创建企业
- ✅ 成员可加入企业
- ✅ 切换空间后数据隔离
- ✅ 成员角色可设置

### 5.2 支付系统

**任务描述**：实现微信支付对接，完成订阅购买和自动续费。

**涉及文件**：
- `/Users/quanyimin/Documents/易合同 AI 助手/app/api/payment/create-order/route.ts`（验证）
- `/Users/quanyimin/Documents/易合同 AI 助手/app/api/payment/wechat/notify/route.ts`（验证）
- `/Users/quanyimin/Documents/易合同 AI 助手/app/pricing/page.tsx`（完善）

**功能要求**：
- 选择套餐 → 生成订单 → 扫码支付 → 激活订阅
- 微信支付回调处理
- 订阅到期自动降级（年付到期→降级到免费版）
- 订单记录 + 发票申请

**验收标准**：
- ✅ 微信支付完整流程跑通
- ✅ 支付后订阅立即生效
- ✅ 到期自动降级
- ✅ 订单可查

### 5.3 电子签章集成

**任务描述**：对接 e签宝 API，实现合同在线签署。

**涉及文件**：
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/lib/esign.ts`
- 新增 `/Users/quanyimin/Documents/易合同 AI 助手/app/api/esign/create/route.ts`
- `/Users/quanyimin/Documents/易合同 AI 助手/app/dashboard/contracts/[id]/page.tsx`（增加签署按钮）

**功能要求**：
- 合同详情页增加「发起签署」按钮
- 选择签署方 → 发送签署邀请 → 在线签署 → 下载已签文件
- 签署状态同步（待签/已签/拒绝）

**验收标准**：
- ✅ 合同可发起电子签署
- ✅ 签署状态实时同步
- ✅ 已签合同可下载

---

## 工期汇总

| 阶段 | 周期 | 开始 | 结束 | 交付物 |
|------|------|------|------|--------|
| **Phase 1** 清理加固 | 5天 | 7/09 | 7/15 | 构建零错误+统一API |
| **Phase 2** 数据模型 | 5天 | 7/16 | 7/22 | Schema就绪+迁移执行 |
| **Phase 3** 个人版MVP | 10天 | 7/23 | 8/05 | **搜索+导入+Onboarding** |
| **Phase 4** 插件完善 | 10天 | 8/06 | 8/19 | 房东+餐饮插件完整 |
| **Phase 5** 企业起步 | 10天 | 8/20 | 9/02 | 企业空间+支付+电子签 |

---

## 严格禁止事项

```
❌ 不在清单里的功能不做（如：不做律所/科技插件、不做数据报表）
❌ 不在当前阶段的任务不做（如：Phase 1 不做搜索）
❌ 不改动已有正常工作流程（如：不动认证逻辑，除非 Phase 1 明确列出）
❌ 不重构正在工作的代码（如：不动 AI 解析层，它在生产环境正常）
❌ 不添加第三方依赖除非必要（如：不引入新的 UI 库）
❌ 不创建任何 .md 文档（已在完成体系文件中列出）
❌ 不改动 pricing 页面（当前只是展示页，等支付对接再改）
❌ 不改动 middleware 认证逻辑（等 Phase 5 企业版再启用）
```

---

## 每日开发规范

```
开始工作前：
  ✔ 读一遍当前 Phase 的任务清单，确认今天要做哪个任务

编码中：
  ✔ 只改任务清单中列出的文件
  ✔ 每次改完一个任务就提交一次（git commit）
  ✔ 提交信息按规范写（feat/fix/refactor/chore）

完成一个任务后：
  ✔ 对照验收标准逐条检查
  ✔ 所有检查通过 → 标记任务为完成
  ✔ 所有检查不通过 → 继续修复，不跳到下一个任务

遇到问题时：
  ✔ 如果是任务本身不清晰 → 停在原地问我
  ✔ 如果是技术实现问题 → 在当前文件范围内找最优解
  ✔ 如果是需要新增文件（不在清单里）→ 先问我
```

---

## 附录：Phase 1 启动步骤（现在就做）

```
Step 1: 删除重复文件
  rm -rf /Users/quanyimin/Documents/易合同\ AI\ 助手/_duplicates_raw

Step 2: 恢复 TypeScript 检查
  打开 next.config.mjs
  删除 typescript: { ignoreBuildErrors: true }, 这一行

Step 3: 构建并修复
  npx next build 2>&1 | tee build-errors.log
  → 按文件逐条修复类型错误
  → 修复后再次构建直到零错误

Step 4: 恢复 ESLint 检查
  打开 next.config.mjs
  删除 eslint: { ignoreDuringBuilds: true }, 这一行
  npx next build 确认零警告
```
